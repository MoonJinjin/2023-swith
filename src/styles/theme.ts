import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  colors: {
    mainBackground: '#', // 밝은 노란색
    subBackground: '#', // 짙은 노란색
    primaryText: '#', // 짙은 회색
    subText: '#', // 짙은 남색
    secondaryText: '#', // 옅은 회색
  },
};

export { theme };
