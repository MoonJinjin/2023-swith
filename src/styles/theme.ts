import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  colors: {
    mainBackground: '#FFF8DB', // 밝은 노란색
    subBackground: '#FCD121', // 짙은 노란색
    primaryText: '#', // 짙은 회색
    subText: '#0F0071', // 짙은 남색
    secondaryText: '#', // 옅은 회색
  },
};

export { theme };
